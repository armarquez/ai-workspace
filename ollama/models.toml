# Pinned local models. Tags verified against https://ollama.com/library/<name>
# before adding — do not guess a tag from memory.
#
# "default" is what `just local` and `just ollama pull` (no args) use.

default = "qwen3.8:27b"

[models]
qwen3.8 = "qwen3.8:27b"       # ~17 GB at q4 — fits 36 GB unified memory
